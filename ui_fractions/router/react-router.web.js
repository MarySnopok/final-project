import { BrowserRouter, HashRouter, Route as WebRoute, Link as WebLink, Routes } from "react-router-dom";

export const Router = HashRouter;
export const Route = WebRoute;
export const Link = WebLink;
export { Routes };
