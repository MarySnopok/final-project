import { NativeRouter, Route as NativeRoute, Link as NativeLink, Routes } from "react-router-native";

export const Router = NativeRouter;
export const Route = NativeRoute;
export const Link = NativeLink;
export { Routes };
